version: '3.8'

services:
  wan-control:
    build:
      context: .
      dockerfile: Dockerfile
    image: wan-emulator-control:latest
    container_name: wan-emulator-control
    ports:
      - "3000:3000"
    restart: unless-stopped
    environment:
      - PORT=3000
    # Optional: run as non-root
    # user: "node"
