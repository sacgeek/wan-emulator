version: '3.8'

services:
  wan-control:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: wan-emulator-control
    ports:
      - "3000:3000"
    restart: unless-stopped
    environment:
      - PORT=3000
